# frozen_string_literal: true

RSpec.describe Simple::Payroll::Calculator do
  it "has a version number" do
    expect(Simple::Payroll::Calculator::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
